camera_set_view_size(view_camera[0], testing, testing);
if(room = rm_game)
{
spawners = instance_number(obj_spawner) + instance_number(obj_spawners)
enemies = instance_number(obj_enemy)
if(spawners == 0 and enemies == 0)
{
room_goto(rm_start)	
}
}


